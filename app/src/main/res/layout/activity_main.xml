<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/editAnotacao"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text=""
        android:hint="Digite as suas anotações"
        android:gravity="start"
        android:padding="15dp"
        android:textSize="18sp"
        android:background="@color/white"
        android:textColor="@color/black"
        android:singleLine="false"
        android:inputType="textMultiLine"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintTop_toBottomOf="parent"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fb_Salvar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        app:layout_constraintHorizontal_bias="0.85"
        app:layout_constraintVertical_bias="0.868"
        app:srcCompat="@drawable/ic_salvar"
        app:layout_constraintStart_toStartOf="@+id/editAnotacao"
        app:layout_constraintEnd_toEndOf="@+id/editAnotacao"
        app:layout_constraintTop_toTopOf="@+id/editAnotacao"
        app:layout_constraintBottom_toBottomOf="@+id/editAnotacao"
        android:src="@drawable/ic_salvar"
        />

</androidx.constraintlayout.widget.ConstraintLayout>